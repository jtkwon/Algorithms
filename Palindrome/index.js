/* Palindromes are strings that form same words when revered.
Return true if Palindrome
*/

const palindrome1 = str =>
  str ===
  str
    .split("")
    .reverse()
    .join("");

const palindrome2 = str => str.split("").every((val) => )

exports.palindrome1 = palindrome1;